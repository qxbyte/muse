# Muse 操作手册

> 面向最终用户的"配置 + 命令 + 场景"参考。新装好 muse → 跟着 §1-2 跑通;有具体诉求 → 查 §3-7;不确定怎么用 → §9 找最近的场景。
>
> 文档版本对齐 muse `v0.1.3`。配置覆盖**当前已实装**的字段;后续模块(上下文管理工程 / Agent 记忆系统 / 向量索引 等)落地时会同步追加章节。
>
> 设计文档(架构 / 路线图 / 决策依据)不在本文档范围,见仓库内的设计文档库索引。

---

## 1. 安装与启动

### 1.1 安装

```bash
# 全局(任意目录都能跑 muse 命令)
npm i -g @qxbyte/muse

# 或仅本仓库使用(项目内 devDependency)
npm i -D @qxbyte/muse
npx muse
```

要求:Node.js >= 20。

### 1.2 首次启动

```bash
muse                          # 进入交互 TUI
muse "总结一下 README"          # 一次性 prompt(类似 cat README | xargs llm)
muse --version                # 打印版本
```

**首次启动报错 `No model configured` / `Active model not found`** — 跳到 §7 配置模型仓库。

### 1.3 交互模式快速键

| 键 | 作用 |
|---|---|
| `Enter` | 提交 |
| `\` + `Enter` | 多行换行 |
| `Esc` | 取消当前 LLM / 工具调用,或清空引导队列 |
| `Esc Esc` | rewind:把最后一条 user message 放回输入框,删除其后所有消息 |
| `Shift+Tab` | 循环切换权限模式(详见 §5) |
| `Ctrl+C` | 退出(或先取消正在跑的工具) |
| `↑` / `↓` | 历史输入翻页 |
| `Cmd+V` / `Ctrl+V` | 粘贴文本;若剪贴板含图自动转 `[Image #N]` 附件 |
| `@<path>` | 文件引用补全:输入 `@src/` 弹出补全 overlay,选中后自动展开文件内容 |
| `/` | Slash 命令补全 |

---

## 2. 配置体系

### 2.1 5 层叠加(由低到高,高覆盖低)

```
1. 内置默认值                          (代码里 DEFAULT_* 常量)
2. ~/.muse/settings.json               全局
3. <cwd>/.muse/settings.json           项目(可入 git)
4. <cwd>/.muse/settings.local.json     项目本地(chmod 600,自动 gitignore)
5. 环境变量 MUSE_*
6. CLI flags(-m / -p / --mode 等)
```

**不配 = 用默认**。只在想改默认时才写对应字段。绝大多数用户只需 `~/.muse/settings.json` 中配 `llm.model` + 一个 `models.local.json`。

### 2.2 文件位置

| 文件 | 用途 |
|---|---|
| `~/.muse/settings.json` | 跨项目的全局偏好 |
| `~/.muse/settings.local.json` | 全局密钥兜底(chmod 600) |
| `~/.muse/models.local.json` | 模型仓库(详见 §7) |
| `<project>/.muse/settings.json` | 团队共享项目配置(入 git) |
| `<project>/.muse/settings.local.json` | 个人项目配置 + 密钥(不入 git) |
| `~/.muse/projects/<hash>/sessions/*.jsonl` | 会话历史 |
| `~/.muse/projects/<hash>/memory/` | 长期 memory(详见 §8) |
| `~/.muse/logs/<date>.jsonl` | `--debug` 模式日志 |

### 2.3 `${ENV_VAR}` 占位符

settings.json 任意字符串字段可写 `${ENV_VAR_NAME}`,加载时展开。未设置的环境变量保留原样。

```jsonc
{
  "providers": {
    "deepseek": { "apiKey": "${DEEPSEEK_API_KEY}" }
  }
}
```

---

## 3. Settings 字段大全(已实装)

> 所有字段**可选**;不写 = 默认。下方默认值标在注释里。

### 3.1 `llm` — 主模型配置

```jsonc
{
  "llm": {
    "model": "deepseek-chat",       // 必填一个;对应 models.local.json 的 entry id
    "provider": "deepseek",         // 仅 fallback 路径用(无 models.local.json 时)
    "temperature": 0.7,             // 0~2,默认由 provider 决定
    "maxTokens": 4096               // 输出最大 token,默认由 provider 决定
  }
}
```

### 3.2 `providers` — provider 凭据(仅 fallback 路径用)

主推使用 §7 `models.local.json`;这里仅在没有 registry 时作 fallback。

```jsonc
{
  "providers": {
    "deepseek": {
      "apiKey": "${DEEPSEEK_API_KEY}",
      "baseUrl": "https://api.deepseek.com",
      "extraHeaders": { "X-Custom": "..." }
    }
  }
}
```

### 3.3 `permissions` — 工具权限规则

```jsonc
{
  "permissions": {
    "allow": ["Read", "Grep", "Bash(npm test)"],     // 自动放行
    "ask":   ["Edit", "Write"],                      // 弹确认
    "deny":  ["Bash(rm)", "Bash(sudo:*)"],           // 自动拒绝
    "defaultMode": "ask"                              // ask | strict | relaxed
  }
}
```

**Pattern 格式**:
- `"ToolName"` — 精确工具名(`Read` / `Edit` / 等)
- `"Bash(<prefix>)"` — Bash 工具 + 命令前缀(如 `Bash(git status)`)
- `"Bash(<prefix>:*)"` — 通配后续参数(如 `Bash(npm:*)` 放行所有 npm 命令)

**硬约束**:`rm -rf` / `sudo` / `curl|sh` 等危险命令由 Bash 工具内部 `HARD_DENY_PATTERNS` 拦截,**所有权限模式都绕不过**。

### 3.4 `preprocess` — 4 段消息预处理管道

```jsonc
{
  "preprocess": {
    "input": {
      "atFileExpand": { "enabled": true, "maxBytes": 1048576 },  // @ 文件引用展开
      "templateExpand": { "enabled": true },                      // 模板展开
      "maxChars": 100000,                                         // 输入长度上限
      "redactPreScan": { "enabled": false }                       // 输入侧密钥扫描
    },
    "request": {
      "trimHistory": { "enabled": true, "budgetRatio": 0.8 },     // 滑窗裁剪触发阈值
      "budgetGuard": { "enabled": true, "budgetRatio": 0.95 },    // 兜底压缩触发阈值
      "redact": { "enabled": false }                              // 出口侧密钥扫描(P2)
    },
    "result": {
      "truncate": { "budgetBytes": 30000 },                       // 单条 tool result 上限
      "detectBinary": { "enabled": true },                        // 二进制检测
      "summarize": { "enabled": true },                           // 自动生成 summary
      "normalizeError": { "enabled": true },                      // 错误归一化
      "redact": { "enabled": true },                              // 结果侧密钥脱敏
      "injectDiff": true                                          // Edit/Write 注入 diff
    },
    "render": {
      "streamMarkdown": { "enabled": true },                      // 流式 markdown 渲染
      "collapseLong": { "enabled": true, "threshold": 200 }       // 长 message 折叠阈值
    },
    "disable": ["redact-pre-scan"]                                // 全局禁用特定 stage(kebab-case)
  }
}
```

### 3.5 `hooks` — 9 个生命周期钩子

```jsonc
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Bash",                          // 工具名 regex(可选,默认匹配所有)
        "command": "/path/to/audit.sh",              // shell 命令,stdin 收 JSON ctx
        "timeout": 5000,                             // ms,默认 30000
        "onError": "skip"                            // skip | throw,默认 throw
      }
    ],
    "PostToolUse": [...],
    "PreCompact": [...],            // 可 block compaction
    "PostCompact": [...],           // 不阻断
    "SessionStart": [...],          // 返回字符串注入 system prompt
    "SessionEnd": [...],
    "UserPromptSubmit": [...],
    "PreLLMRequest": [...],
    "PostLLMResponse": [...]
  }
}
```

Hook 子进程**走环境变量白名单**(避免泄露密钥),`settings.env` 注入的变量不会自动透传。

### 3.6 `ui` — 界面偏好

```jsonc
{
  "ui": {
    "theme": "dark",          // dark | light (light 暂未实装)
    "lang": "zh-CN",          // zh-CN | en
    "showBanner": true        // 启动 banner
  }
}
```

### 3.7 `memory` — 长期记忆配置

muse memory 分**两层独立存储 + 合并召回**:

| scope | 路径 | 范围 |
|---|---|---|
| `project` | `~/.muse/projects/<hash>/memory/` | 当前项目专属 |
| `user` | `~/.muse/memory/` | 跨项目用户级 |

召回时两层合并,**项目优先**(同分时 project 排前)。

**写入入口**:
- LLM 在对话里自主 `MemoryWrite`(默认 `scope=project`,个人偏好类强制 `scope=user`)
- 用户 `/remember <text>` 显式触发(默认 `scope=user`,详见 §4.4)
- `/compact` 触发的事实自动 promote 到 memory(`scope=project`)

**配置字段**:

```jsonc
{
  "memory": {
    "embedding": {
      "enabled": false,                       // 默认关闭;开启后每轮按用户消息向量召回 top-K
      "provider": "hash-bag",                 // hash-bag(默认,零依赖)
                                              // local-minilm / openai 留下批
      "model": "Xenova/bge-small-zh-v1.5",   // local-minilm 用
      "apiKey": "${OPENAI_API_KEY}",          // openai 用
      "topK": 5,                              // 每轮注入的 memory 条数上限
      "minMemoryCount": 3,                    // memory 数 < 此值时退化到全文注入
      "maxInjectTokens": 1500                 // 注入 token 预算上限
    }
  }
}
```

**行为说明**(用户视角):

- 启用 embedding 后,muse 启动时构建 `.index.json` 向量索引落到每层 memory 目录
- 每轮 LLM 调用前,基于最近用户消息召回最相关的 top-K memory 注入到 system prompt
- 召回内容按 trust 分级:`trusted` 注入完整 body / `verified` 注入摘要 / `auto` 仅注入索引行 — 这样 token 经济
- 总注入超 `maxInjectTokens` 时优先保留 `trusted`(硬约束),其他按相关性截尾
- memory 数少于 `minMemoryCount` 时自动退化到传统全文注入(几条 memory 时全文更可靠)
- **session 内立即生效**:LLM `MemoryWrite` 或 `/remember` 写完后自动更新索引,当前 session 就能召回新条目
- `muse --debug` 模式下召回结果写日志(`~/.muse/logs/<date>.jsonl`),可用 `jq` 查看召回质量

完整设计细节 / 召回算法 / R1-R6 优化项见模块设计文档(`Muse/模块设计/Agent 记忆系统/设计.md` + `实现.md`)。

### 3.8 `env` — 注入 process.env

```jsonc
{
  "env": {
    "MUSE_DISABLE_CURSOR_BLINK": "1",   // 关闭输入框光标闪烁
    "MUSE_MODEL": "deepseek-chat"       // 覆盖 active model(等价 -m flag)
  }
}
```

值必须是字符串(JSON 无 number→env 隐式转换;约束传 `"1"` / `"0"` 字面值)。

### 3.8 已声明但未实装(占位)

| 字段 | 状态 |
|---|---|
| `mcpServers` | 留 v0.3 — MCP 接入未实装 |
| `skills` | 留 v0.3 — skill 系统未实装 |

写入这两个字段当前 **no-op**(不报错也不生效),供未来配置兼容。

---

## 4. Slash 命令大全

| 命令 | 别名 | 作用 |
|---|---|---|
| `/help` | — | 列出所有 slash 命令 |
| `/clear` | — | 清空当前会话历史(memory / models registry 不动) |
| `/exit` | — | 退出 muse |
| `/cost` | — | 显示本会话累计 token + 估算 USD |
| `/compact` | `/compact --keep <N>` | 主动压缩对话历史(默认保留最近 4 条) |
| `/model` | — | 弹出 model selector;切到 `~/.muse/models.local.json` 里另一个 entry |
| `/config reload` | — | 重新加载 settings.json + models.local.json(改完不用退 muse) |
| `/config show` | — | 显示当前生效配置 + 来源链 |
| `/mcp` | — | 显示 MCP servers 状态(当前未实装,显示 stub) |
| `/memory` | `/memory <sub>` | 长期记忆管理(详见 §4.3) |
| `/remember` | `<text>` | 让 LLM 把口语化文本结构化后存入 memory(默认 scope=user) |
| `/resume` | — | 列出可恢复的历史 session,选一个继续 |
| `/mode <mode>` | — | 切权限模式(default / acceptEdits / plan / bypassPermissions) |
| `/btw` | — | 旁白问答 — 临时浮层,不入会话历史 |

### 4.1 `/compact` 详解

```
/compact                       # 默认保留最近 4 条 + LLM 摘要前面
/compact --keep 8              # 保留最近 8 条
```

**触发条件**:
- 自动:`budgetGuard` 阈值(默认 0.95)超出时
- 手动:本命令

**hooks**:`PreCompact`(可 block) / `PostCompact`(不阻断)

### 4.3 `/memory` 子命令

```
/memory                                # 默认 = list(合并两层)
/memory list [--scope project|user|all]  # 列 memory(默认 all)
/memory view <name> [--scope <s>]      # 显示 frontmatter + body(重名时需 --scope 消歧)
/memory delete <name> [--scope <s>]    # 删除
/memory promote <name>                 # auto → verified(trust 升级)
/memory promote-scope <name>           # project → user(scope 升级,跨项目生效)
/memory trust <name> <level>           # 显式改 trust(verified | auto)
/memory search <query>                 # 向量索引召回 top-K(显示 scope tag)
/memory help                           # 帮助
```

**`/memory search` 说明**:基于 EmbeddingProvider 接口的余弦相似度召回。本期默认 `hash-bag` provider(本地零依赖,英文按词 / 中文按字 + DJB2 哈希到 128 维),适合关键词重叠类查询。真 embedding model(`local-minilm` MiniLM-L6-v2 / `openai` text-embedding-3-small)留下批引入。trust 加权:`trusted ×1.5 / verified ×1.2 / auto ×1.0`,同分时 trust 高排前。

**trust 三级**:
- `trusted` — 来自 `MUSE.md` / `AGENTS.md` / managed config;视为硬约束
- `verified` — 用户编辑 / 显式 promote;视为确认知识
- `auto` — LLM 自动写入,未审核;视为提示线索,新证据可覆盖

**trust 升降规则**:只升不降。一旦升到 `verified`,无法用 `/memory trust ... auto` 降回。`trusted` 是 hierarchy 文件专属(MUSE.md / AGENTS.md),slash 命令不能设置。

**未实装(留下批)**:`edit`(外部编辑器)/ `diff`(git 集成)/ `search`(向量召回需启用 embedding)。

### 4.4 `/remember` — 用户显式记忆入口

```
/remember [--user|--project] <text>
```

**默认 `scope=user`**(用户主动 `/remember` 通常是跨项目偏好)。muse 调当前 LLM 把口语化文本结构化为 `{ name, type, description, body }`,写入 `trust=verified` + `source=user-remember`。启用 embedding 时自动 upsert,**当前 session 立即可召回**。

**示例**:

```
> /remember I prefer pnpm over npm in all projects
Created memory "prefer-pnpm-over-npm"
  scope:       user
  type:        user
  trust:       verified (source: user-remember)
  description: User prefers pnpm package manager over npm across projects.
  → ~/.muse/memory/prefer-pnpm-over-npm.md

> /remember --project this team uses semantic-release for versioning
Created memory "team-uses-semantic-release"
  scope:       project
  ...
```

**与 `MemoryWrite`(LLM 自主)的区别**:`/remember` 是用户主动,自动 `trust=verified`;`MemoryWrite` 由 LLM 在对话中判断写入,默认 `trust=auto`(用户可后续 `/memory promote <name>` 升级)。

### 4.5 `/btw` 用法(旁白问答)

```
/btw 这个 todoStore 的 ephemeral 设计是为什么?
```

LLM 直接回答,**不进会话历史**,用完即焚。适合"我不确定要不要继续这条思路,先临时问一下"场景。

---

## 5. 权限模式(4 档)

`Shift+Tab` 循环切换;启动时 `--mode <mode>` 指定;`/mode <mode>` 命令切换。

| 模式 | 行为 | 适用场景 |
|---|---|---|
| `default` | 完全走 `settings.permissions` 规则 | 日常 |
| `acceptEdits` | `Edit` / `Write` 自动 allow,其他走 default | 信任改动,要快 |
| `plan` | **只允许 read 类工具**(Read / Grep / Glob / MemoryRead / WebFetch),写类全 deny | 分析 / 调研 / 让 LLM 给方案 |
| `bypassPermissions` | 除显式 deny 与 Bash 硬拒绝外**全 allow** | 完全信任 / 沙盒环境 |

**注意**:
- 模式**不持久化** — 关 muse 重开回 `default`
- `--mode bypassPermissions` 启动相当于"无管控",仅在你确定的环境用
- 三态规则(allow / ask / deny)+ 4 档模式正交叠加

---

## 6. CLI flags

```
muse [options] [prompt...]

  -v, --version                     打印版本
  -h, --help                        打印帮助
  -m, --model <model>               覆盖 active model(等价 settings.llm.model)
  -p, --provider <provider>         覆盖 active provider
      --no-banner                   跳过启动 banner
      --quiet                       最少输出(隐含 --no-banner)
      --continue                    恢复当前目录最近一次 session
      --mode <mode>                 启动权限模式 (default|acceptEdits|plan|bypassPermissions)
      --debug                       详细日志写 ~/.muse/logs/<date>.jsonl
```

**一次性 prompt 模式**:任何位置参数都被当 prompt。

```bash
muse "总结一下 src/cli.tsx"
echo "解释这段代码" | muse                 # stdin pipe
muse --quiet "list TODO comments" | tee out.txt
```

---

## 7. 模型仓库(`~/.muse/models.local.json`)

muse 的"主推配置路径":所有 LLM 接入信息(凭据 / baseUrl / 能力)收在这一份文件里。

### 7.1 文件位置与权限

```bash
~/.muse/models.local.json
# 建议 chmod 600(包含明文 / 占位 API key)
chmod 600 ~/.muse/models.local.json
```

**绝不**入 git(后缀 `.local.json` 本身就是视觉提示)。

### 7.2 结构

```jsonc
{
  "models": [
    {
      "id": "deepseek-chat",                              // 用户起的名字;/model selector 显示这个
      "name": "DeepSeek Chat",                            // 可选;UI 显示用
      "vendor": "deepseek",                               // 可选;分组标签
      "apiKey": "${DEEPSEEK_API_KEY}",                    // 明文或 ${ENV_VAR}
      "baseUrl": "https://api.deepseek.com",              // 基址,SDK 拼 /chat/completions
      "supportsToolCall": true,                           // 模型是否支持 function call
      "supportsImages": false,                            // 是否支持图片 input
      "contextWindow": 128000                             // token 数;trim / compact 用
    },
    {
      "id": "qwen-coder",
      "vendor": "qwen",
      "apiKey": "${DASHSCOPE_API_KEY}",
      "baseUrl": "https://dashscope.aliyuncs.com/compatible-mode/v1",
      "supportsToolCall": true,
      "contextWindow": 131072
    },
    {
      "id": "local-ollama",
      "vendor": "ollama",
      "baseUrl": "http://localhost:11434/v1",
      "supportsToolCall": false
    }
  ]
}
```

### 7.3 常见 vendor 模板

<details>
<summary>展开:DeepSeek / Qwen / Kimi / GLM / Ollama / OpenAI</summary>

```jsonc
{
  "models": [
    // DeepSeek
    {
      "id": "deepseek-chat",
      "vendor": "deepseek",
      "apiKey": "${DEEPSEEK_API_KEY}",
      "baseUrl": "https://api.deepseek.com",
      "supportsToolCall": true,
      "contextWindow": 128000
    },
    // Qwen(阿里灵积 / DashScope)
    {
      "id": "qwen-max",
      "vendor": "qwen",
      "apiKey": "${DASHSCOPE_API_KEY}",
      "baseUrl": "https://dashscope.aliyuncs.com/compatible-mode/v1",
      "supportsToolCall": true,
      "contextWindow": 32768
    },
    // Kimi(月之暗面)
    {
      "id": "kimi-k2",
      "vendor": "kimi",
      "apiKey": "${MOONSHOT_API_KEY}",
      "baseUrl": "https://api.moonshot.cn/v1",
      "supportsToolCall": true,
      "contextWindow": 200000
    },
    // GLM(智谱)
    {
      "id": "glm-4-plus",
      "vendor": "glm",
      "apiKey": "${ZHIPU_API_KEY}",
      "baseUrl": "https://open.bigmodel.cn/api/paas/v4",
      "supportsToolCall": true,
      "contextWindow": 128000
    },
    // Ollama(本地)
    {
      "id": "local-qwen",
      "vendor": "ollama",
      "baseUrl": "http://localhost:11434/v1",
      "supportsToolCall": true,
      "contextWindow": 32768
    },
    // OpenAI
    {
      "id": "gpt-4o",
      "vendor": "openai",
      "apiKey": "${OPENAI_API_KEY}",
      "baseUrl": "https://api.openai.com/v1",
      "supportsToolCall": true,
      "supportsImages": true,
      "contextWindow": 128000
    }
  ]
}
```

</details>

### 7.4 切模型

启动后:`/model` → picker → ↑↓ 选择 → Enter。切换不重启 muse。

启动时:`muse -m gpt-4o`。

---

## 8. Memory(当前版本)

### 8.1 基础概念

| 类型 | 用途 |
|---|---|
| `user` | 用户角色 / 偏好 |
| `feedback` | 验证过的方法 / 反模式 |
| `project` | 项目事实 / 决策 |
| `reference` | 外部系统指针(URL / 路径) |

LLM 自主调 `MemoryWrite` 写入;`MEMORY.md` 索引前 200 行自动注入 system prompt。

### 8.2 文件位置

```
~/.muse/projects/<sha256(cwd)[:16]>/memory/
  MEMORY.md             索引
  user_*.md             user 类
  feedback_*.md         feedback 类
  project_*.md          project 类
  reference_*.md        reference 类
```

每个 .md 带 frontmatter:

```yaml
---
name: user-prefers-tabs
description: 用户偏好制表符缩进
metadata:
  type: feedback
---

(body)
```

### 8.3 手动编辑

直接打开文件改即可,muse 下次启动自动读取。**不要手改 `MEMORY.md` 索引** — 用 `MemoryWrite` 写新条目时索引会自动更新,手改可能在下一轮被覆盖。

### 8.4 后续(v0.2.x)

`MUSE.md` + `AGENTS.md` 5 层 hierarchy / `/memory` slash / trust 分级 / 可选向量索引 — 见设计文档库,实装上线后追加章节。

---

## 9. 常用场景示例

### 9.1 接入 DeepSeek + 默认配 muse

```bash
# 1. 准备 API key
export DEEPSEEK_API_KEY=sk-xxx
echo 'export DEEPSEEK_API_KEY=sk-xxx' >> ~/.zshrc

# 2. 写模型仓库
mkdir -p ~/.muse
cat > ~/.muse/models.local.json <<'EOF'
{
  "models": [
    {
      "id": "deepseek-chat",
      "vendor": "deepseek",
      "apiKey": "${DEEPSEEK_API_KEY}",
      "baseUrl": "https://api.deepseek.com",
      "supportsToolCall": true,
      "contextWindow": 128000
    }
  ]
}
EOF
chmod 600 ~/.muse/models.local.json

# 3. 选默认模型
cat > ~/.muse/settings.json <<'EOF'
{
  "llm": { "model": "deepseek-chat" }
}
EOF

# 4. 启动
muse
```

### 9.2 团队 + 个人配置分离

```bash
# 团队共享(入 git)
cat > <project>/.muse/settings.json <<'EOF'
{
  "permissions": {
    "deny": ["Bash(npm publish:*)", "Bash(git push:*)"]
  },
  "preprocess": {
    "result": {
      "truncate": { "budgetBytes": 20000 }
    }
  }
}
EOF

# 个人本地(不入 git,密钥放这里)
cat > <project>/.muse/settings.local.json <<'EOF'
{
  "llm": { "model": "kimi-k2" },
  "env": {
    "PROJECT_DB_URL": "postgres://localhost/mydb"
  }
}
EOF
chmod 600 <project>/.muse/settings.local.json

# 自动加 gitignore
echo '.muse/settings.local.json' >> <project>/.gitignore
```

### 9.3 用 plan 模式让 LLM 给方案不直接动手

```bash
muse --mode plan "如何把当前测试套件拆成 unit/integration 两层?"
```

或启动后 `Shift+Tab` 循环到 plan,提问完看方案。

### 9.4 上下文吃紧时手动 compact

```
> /compact --keep 6
✦ Compacting... (3s)
[Compacted: 34 messages → 1 summary + 6 messages]
```

### 9.5 配 Hook 在每次 Bash 调用前审计

```jsonc
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^Bash$",
        "command": "/usr/local/bin/audit-bash.sh",
        "timeout": 3000,
        "onError": "skip"
      }
    ]
  }
}
```

`audit-bash.sh` 从 stdin 读 JSON `{toolName, args, mode}`,exit 0 = 放行 / 非 0 = block。

### 9.6 接 Ollama 本地模型(免 API)

```jsonc
{
  "models": [
    {
      "id": "local-qwen",
      "vendor": "ollama",
      "baseUrl": "http://localhost:11434/v1",
      "supportsToolCall": true,
      "contextWindow": 32768
    }
  ]
}
```

```bash
ollama pull qwen2.5-coder
muse -m local-qwen
```

### 9.7 启用向量索引召回 memory

```jsonc
{
  "memory": {
    "embedding": {
      "enabled": true,
      "provider": "hash-bag",     // 本地零依赖默认;真 embedding 后端留下批引入
      "topK": 5,
      "minMemoryCount": 10
    }
  }
}
```

启用后:
- 启动时 muse 自动 build 索引(memory 少时 < 100ms)
- 每轮 LLM 前根据"最新 user 消息"召回 top-5 相关 memory
- `/memory search <query>` slash 命令可手动测试召回效果
- memory 少于 10 条时自动退化到传统全文(更稳)

**当前 hash-bag 后端的限制**:按关键词重叠(英文按词 / 中文按字)匹配,**不懂语义**(`"用户偏好制表符"` 与 `"代码缩进风格"` 不会匹配)。真 embedding model(MiniLM / OpenAI)留下批引入。

### 9.8 关闭流式 markdown(纯文本输出,debug 用)

```jsonc
{
  "preprocess": {
    "render": {
      "streamMarkdown": { "enabled": false }
    }
  }
}
```

### 9.9 跑一次性命令把结果保存

```bash
muse --quiet "列出 src/ 下所有 TS 文件的导出名" > exports.txt
```

`--quiet` 跳过 banner + 多余装饰,适合管道。

---

## 10. 故障排查 / FAQ

> 本章目前仅收录已知高频问题,后续按用户反馈补充。

### 10.1 `No model configured` / `Active model "X" not found`

`~/.muse/models.local.json` 缺失或 `settings.llm.model` 跟 registry id 对不上。三选一:
1. 编辑 `~/.muse/settings.json`:把 `llm.model` 改成 registry 已有 id
2. 编辑 `~/.muse/models.local.json`:把某 entry id 改成 settings 中的值
3. 用 `muse --model <id>` 临时覆盖

### 10.2 token 用爆 / 模型报 context exceeded

- 检查 `models.local.json` 里该 entry 的 `contextWindow` 是否设对了
- 改严 `preprocess.request.trimHistory.budgetRatio`(默认 0.8,可降到 0.7)
- 改严 `preprocess.result.truncate.budgetBytes`(默认 30000,可降到 20000)
- 手动 `/compact --keep 4`

### 10.3 修改 settings.json 不生效

- 看是不是改错了文件(`~/.muse/` vs `<cwd>/.muse/`)
- `/config show` 看实际生效来源
- `/config reload` 不重启 muse 重读

### 10.4 启动卡住 / 闪屏 / 渲染异常

- 加 `--debug` 跑一次,看 `~/.muse/logs/<date>.jsonl`
- 临时关 stream-markdown(§9.7)看是否渲染层问题
- 设 `MUSE_DISABLE_CURSOR_BLINK=1` 关光标闪烁

### 10.5 API key 泄露 / 想改

- key 仅走 `~/.muse/models.local.json`(明文或 `${ENV_VAR}`)
- 改完无需重启:`/config reload`
- 日志默认脱敏(前 4 后 4,中间星号)

---

## 11. 完整 settings.json 示例

### 11.1 最小可用

```jsonc
{
  "llm": { "model": "deepseek-chat" }
}
```

### 11.2 团队项目模板(`.muse/settings.json`,入 git)

```jsonc
{
  "permissions": {
    "deny": [
      "Bash(rm:*)",
      "Bash(git push:*)",
      "Bash(npm publish:*)"
    ],
    "ask": ["Edit", "Write"]
  },
  "preprocess": {
    "result": {
      "truncate": { "budgetBytes": 25000 }
    }
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Edit|Write)$",
        "command": "./.muse/hooks/audit-edit.sh",
        "timeout": 5000,
        "onError": "skip"
      }
    ]
  }
}
```

### 11.3 调试 / 实验模板(`~/.muse/settings.json`)

```jsonc
{
  "llm": { "model": "deepseek-chat" },
  "preprocess": {
    "render": {
      "streamMarkdown": { "enabled": false },
      "collapseLong": { "enabled": false }
    }
  },
  "env": {
    "MUSE_DISABLE_CURSOR_BLINK": "1"
  },
  "ui": {
    "showBanner": false
  }
}
```

---

## 附录

### A. 已实装功能清单(对照设计文档)

详见仓库内设计文档库的"功能清单"。本文档章节标 ✅ 的字段当前可用;留 v0.2.x / v0.3 的功能(MCP / skills / 多 hierarchy memory / 向量索引 / etc.)在路线图中。

### B. 反馈渠道

GitHub Issues:[https://github.com/qxbyte/muse/issues](https://github.com/qxbyte/muse/issues)
